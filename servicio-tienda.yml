app:
  client_id: validador
  grant_type: password
  password: admin
  urlAuth: http://${HOST}:8080/realms/UsuariosCursoRealm/protocol/openid-connect/token
  username: admin
server:
  port: 7000
eureka:
 client:
  serviceUrl:
   defaultZone: http://${HOST}:8761/eureka
