app:
  client_id: validador
  grant_type: password
  password: admin
  urlAuth: http://${HOST}/realms/UsuariosCursoRealm/protocol/openid-connect/token
  username: admin
server:
  port: 7000
eureka:
  client:
    serviceUrl:
      defaultZone: http//${HOST}/eureka
